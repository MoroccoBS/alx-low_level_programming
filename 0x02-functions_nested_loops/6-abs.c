#include "main.h"
/**
 * int_abs - computes the absolute value of an integer
 * @: integer to be checked
 * Return: int
 */
int int_abs(int)
{
	if (int >= 0)
		return (int);
	else 
		return (int * -1);
}
